<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CarExGame - Ultimate Car Racing Experience</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        :root {
            --primary: #ff4757;
            --secondary: #2ed573;
            --accent: #3742fa;
            --dark: #1e272e;
            --light: #f1f2f6;
            --gradient: linear-gradient(135deg, #ff6b6b, #ff4757, #ff3838);
            --gradient-secondary: linear-gradient(135deg, #2ed573, #1e90ff);
            --card-bg: rgba(255, 255, 255, 0.08);
            --card-border: rgba(255, 255, 255, 0.1);
            --shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        body {
            background: linear-gradient(135deg, #0c2461, #1e3799, #4a69bd);
            color: var(--light);
            overflow-x: hidden;
            line-height: 1.6;
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 10px;
        }

        ::-webkit-scrollbar-track {
            background: rgba(0, 0, 0, 0.2);
        }

        ::-webkit-scrollbar-thumb {
            background: var(--gradient);
            border-radius: 5px;
        }

        /* Header */
        header {
            background: rgba(30, 39, 46, 0.95);
            padding: 20px 0;
            position: fixed;
            width: 100%;
            z-index: 1000;
            box-shadow: var(--shadow);
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }

        .container {
            width: 90%;
            max-width: 1200px;
            margin: 0 auto;
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .logo-icon {
            font-size: 28px;
            background: var(--gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .logo h1 {
            font-size: 28px;
            font-weight: 800;
            background: var(--gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        nav ul {
            display: flex;
            list-style: none;
        }

        nav ul li {
            margin-left: 30px;
            position: relative;
        }

        nav ul li a {
            color: var(--light);
            text-decoration: none;
            font-weight: 600;
            font-size: 16px;
            transition: color 0.3s;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        nav ul li a i {
            font-size: 14px;
        }

        nav ul li a:hover {
            color: var(--primary);
        }

        nav ul li a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -5px;
            left: 0;
            background: var(--gradient);
            transition: width 0.3s;
        }

        nav ul li a:hover::after {
            width: 100%;
        }

        .header-actions {
            display: flex;
            gap: 15px;
        }

        .btn-login, .btn-signup {
            padding: 8px 20px;
            border-radius: 30px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }

        .btn-login {
            background: transparent;
            border: 2px solid var(--primary);
            color: var(--light);
        }

        .btn-signup {
            background: var(--gradient);
            border: none;
            color: white;
            box-shadow: 0 4px 15px rgba(255, 71, 87, 0.4);
        }

        .btn-login:hover {
            background: rgba(255, 71, 87, 0.1);
        }

        .btn-signup:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 20px rgba(255, 71, 87, 0.6);
        }

        /* Hero Section */
        .hero {
            height: 100vh;
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .hero-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('https://images.unsplash.com/photo-1549317661-bd32c8ce0db2?ixlib=rb-1.2.1&auto=format&fit=crop&w=1350&q=80') no-repeat center center/cover;
            z-index: -2;
        }

        .hero-bg::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(12, 36, 97, 0.9), rgba(30, 55, 153, 0.8));
            z-index: -1;
        }

        .floating-car {
            position: absolute;
            right: 10%;
            bottom: 10%;
            font-size: 300px;
            color: rgba(255, 255, 255, 0.1);
            z-index: -1;
            animation: float 6s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .hero-content {
            max-width: 700px;
            z-index: 1;
        }

        .hero h2 {
            font-size: 52px;
            margin-bottom: 20px;
            line-height: 1.2;
            background: linear-gradient(90deg, #fff, #ffd700);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .hero p {
            font-size: 18px;
            margin-bottom: 30px;
            line-height: 1.6;
            color: rgba(255, 255, 255, 0.8);
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 14px 35px;
            background: var(--gradient);
            color: white;
            border: none;
            border-radius: 30px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            transition: transform 0.3s, box-shadow 0.3s;
            box-shadow: 0 6px 20px rgba(255, 71, 87, 0.4);
        }

        .btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(255, 71, 87, 0.6);
        }

        .btn-secondary {
            background: var(--gradient-secondary);
            box-shadow: 0 6px 20px rgba(30, 144, 255, 0.4);
        }

        .btn-secondary:hover {
            box-shadow: 0 10px 25px rgba(30, 144, 255, 0.6);
        }

        .hero-btns {
            display: flex;
            gap: 15px;
            flex-wrap: wrap;
        }

        /* Section Styling */
        .section {
            padding: 100px 0;
        }

        .section-title {
            text-align: center;
            margin-bottom: 60px;
        }

        .section-title h2 {
            font-size: 42px;
            position: relative;
            display: inline-block;
            padding-bottom: 15px;
            margin-bottom: 15px;
        }

        .section-title h2::after {
            content: '';
            position: absolute;
            width: 80px;
            height: 4px;
            background: var(--gradient);
            bottom: 0;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 2px;
        }

        .section-title p {
            max-width: 600px;
            margin: 0 auto;
            color: rgba(255, 255, 255, 0.7);
        }

        /* Features */
        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
        }

        .feature-card {
            background: var(--card-bg);
            border-radius: 15px;
            padding: 30px;
            transition: transform 0.3s, box-shadow 0.3s;
            backdrop-filter: blur(10px);
            border: 1px solid var(--card-border);
            position: relative;
            overflow: hidden;
            z-index: 1;
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--gradient);
            opacity: 0;
            transition: opacity 0.3s;
            z-index: -1;
        }

        .feature-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--shadow);
        }

        .feature-card:hover::before {
            opacity: 0.1;
        }

        .feature-icon {
            font-size: 40px;
            margin-bottom: 20px;
            background: var(--gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .feature-card h3 {
            font-size: 22px;
            margin-bottom: 15px;
        }

        /* Game Demo */
        .game-demo {
            background: rgba(0, 0, 0, 0.7);
            padding: 40px;
            border-radius: 20px;
            text-align: center;
            box-shadow: var(--shadow);
            border: 1px solid rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
        }

        .game-container {
            width: 100%;
            height: 500px;
            background: #111;
            border-radius: 15px;
            overflow: hidden;
            position: relative;
            margin: 0 auto 30px;
            border: 2px solid rgba(255, 255, 255, 0.1);
            box-shadow: inset 0 0 20px rgba(0, 0, 0, 0.5);
        }

        .game-road {
            position: absolute;
            width: 100%;
            height: 200%;
            background: #222;
            top: -100%;
            animation: roadMove 3s linear infinite;
        }

        @keyframes roadMove {
            0% { top: -100%; }
            100% { top: 0%; }
        }

        .road-line {
            position: absolute;
            width: 10px;
            height: 80px;
            background: #fff;
            left: 50%;
            transform: translateX(-50%);
        }

        .player-car {
            position: absolute;
            width: 70px;
            height: 140px;
            background: var(--gradient);
            bottom: 50px;
            left: 50%;
            transform: translateX(-50%);
            border-radius: 10px 10px 0 0;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.5);
            transition: left 0.2s;
            z-index: 10;
        }

        .player-car::before {
            content: '';
            position: absolute;
            width: 50px;
            height: 25px;
            background: #222;
            top: 15px;
            left: 10px;
            border-radius: 5px;
        }

        .player-car::after {
            content: '';
            position: absolute;
            width: 60px;
            height: 40px;
            background: rgba(0, 0, 0, 0.3);
            bottom: 0;
            left: 5px;
            border-radius: 5px 5px 0 0;
        }

        .car-window {
            position: absolute;
            width: 40px;
            height: 20px;
            background: #87CEEB;
            top: 20px;
            left: 15px;
            border-radius: 3px;
        }

        .car-light {
            position: absolute;
            width: 10px;
            height: 5px;
            background: yellow;
            bottom: 15px;
        }

        .car-light.left {
            left: 5px;
        }

        .car-light.right {
            right: 5px;
        }

        .obstacle {
            position: absolute;
            width: 70px;
            height: 120px;
            background: var(--gradient-secondary);
            border-radius: 5px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            z-index: 5;
        }

        .obstacle::before {
            content: '';
            position: absolute;
            width: 50px;
            height: 20px;
            background: #222;
            top: 15px;
            left: 10px;
            border-radius: 3px;
        }

        .score-display {
            display: flex;
            justify-content: space-between;
            margin-bottom: 20px;
            font-size: 20px;
            font-weight: 700;
        }

        .score, .high-score {
            background: var(--gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            padding: 5px 15px;
            border-radius: 10px;
            background-color: rgba(255, 255, 255, 0.1);
        }

        .controls {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 20px;
            flex-wrap: wrap;
        }

        .control-btn {
            padding: 12px 25px;
            background: rgba(255, 255, 255, 0.1);
            border: 2px solid var(--primary);
            color: white;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            gap: 8px;
            font-weight: 600;
        }

        .control-btn:hover {
            background: rgba(255, 71, 87, 0.2);
            transform: translateY(-3px);
        }

        .game-instructions {
            margin-top: 20px;
            color: rgba(255, 255, 255, 0.7);
            font-size: 14px;
        }

        /* Cars Section */
        .cars {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
        }

        .car-card {
            background: var(--card-bg);
            border-radius: 15px;
            padding: 25px;
            text-align: center;
            transition: transform 0.3s, box-shadow 0.3s;
            backdrop-filter: blur(10px);
            border: 1px solid var(--card-border);
            position: relative;
            overflow: hidden;
        }

        .car-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--gradient);
            opacity: 0;
            transition: opacity 0.3s;
            z-index: -1;
        }

        .car-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--shadow);
        }

        .car-card:hover::before {
            opacity: 0.1;
        }

        .car-image {
            height: 120px;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .car-icon {
            font-size: 80px;
            background: var(--gradient);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            transition: transform 0.3s;
        }

        .car-card:hover .car-icon {
            transform: scale(1.1);
        }

        .car-card h3 {
            font-size: 20px;
            margin-bottom: 10px;
        }

        .car-stats {
            display: flex;
            justify-content: space-between;
            margin-top: 15px;
        }

        .stat {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .stat-value {
            font-weight: 700;
            font-size: 18px;
            color: var(--secondary);
        }

        .stat-label {
            font-size: 12px;
            color: rgba(255, 255, 255, 0.6);
        }

        /* Footer */
        footer {
            background: rgba(10, 20, 40, 0.95);
            padding: 70px 0 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .footer-content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 40px;
            margin-bottom: 40px;
        }

        .footer-column h3 {
            font-size: 20px;
            margin-bottom: 20px;
            color: var(--primary);
            position: relative;
            padding-bottom: 10px;
        }

        .footer-column h3::after {
            content: '';
            position: absolute;
            width: 40px;
            height: 3px;
            background: var(--gradient);
            bottom: 0;
            left: 0;
            border-radius: 2px;
        }

        .footer-column ul {
            list-style: none;
        }

        .footer-column ul li {
            margin-bottom: 12px;
        }

        .footer-column ul li a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            transition: color 0.3s;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .footer-column ul li a:hover {
            color: var(--primary);
            transform: translateX(5px);
        }

        .social-links {
            display: flex;
            gap: 15px;
            margin-top: 20px;
        }

        .social-link {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            color: white;
            text-decoration: none;
            transition: all 0.3s;
        }

        .social-link:hover {
            background: var(--gradient);
            transform: translateY(-5px);
        }

        .copyright {
            text-align: center;
            padding-top: 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            font-size: 14px;
            color: rgba(255, 255, 255, 0.5);
        }

        /* Game Over Modal */
        .game-over {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 20;
            color: white;
            text-align: center;
            display: none;
        }

        .game-over h2 {
            font-size: 48px;
            margin-bottom: 20px;
            color: var(--primary);
        }

        .game-over p {
            font-size: 24px;
            margin-bottom: 30px;
        }

        /* Responsive Design */
        @media (max-width: 992px) {
            .floating-car {
                font-size: 200px;
                right: 5%;
            }
            
            .hero h2 {
                font-size: 42px;
            }
        }

        @media (max-width: 768px) {
            .header-content {
                flex-direction: column;
                gap: 15px;
            }
            
            nav ul {
                margin-top: 15px;
                flex-wrap: wrap;
                justify-content: center;
            }
            
            nav ul li {
                margin: 0 10px;
            }
            
            .hero h2 {
                font-size: 36px;
            }
            
            .floating-car {
                display: none;
            }
            
            .hero-btns {
                justify-content: center;
            }
            
            .game-container {
                height: 400px;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <div class="header-content">
                <div class="logo">
                    <div class="logo-icon">
                        <i class="fas fa-car-side"></i>
                    </div>
                    <h1>CarExGame</h1>
                </div>
                <nav>
                    <ul>
                        <li><a href="#home"><i class="fas fa-home"></i> Home</a></li>
                        <li><a href="#features"><i class="fas fa-star"></i> Features</a></li>
                        <li><a href="#game"><i class="fas fa-gamepad"></i> Play Game</a></li>
                        <li><a href="#cars"><i class="fas fa-car"></i> Cars</a></li>
                        <li><a href="#contact"><i class="fas fa-envelope"></i> Contact</a></li>
                    </ul>
                </nav>
                <div class="header-actions">
                    <button class="btn-login">Login</button>
                    <button class="btn-signup">Sign Up</button>
                </div>
            </div>
        </div>
    </header>

    <section class="hero" id="home">
        <div class="hero-bg"></div>
        <div class="floating-car">
            <i class="fas fa-car-side"></i>
        </div>
        <div class="container">
            <div class="hero-content">
                <h2>Experience the Ultimate Car Racing Adventure</h2>
                <p>Get behind the wheel of the world's most exotic cars and race through stunning environments. Compete with players worldwide and become the ultimate racing champion!</p>
                <div class="hero-btns">
                    <a href="#game" class="btn"><i class="fas fa-play-circle"></i> Play Now</a>
                    <a href="#features" class="btn btn-secondary"><i class="fas fa-info-circle"></i> Learn More</a>
                </div>
            </div>
        </div>
    </section>

    <section class="section" id="features">
        <div class="container">
            <div class="section-title">
                <h2>Game Features</h2>
                <p>Discover what makes CarExGame the most thrilling racing experience</p>
            </div>
            <div class="features">
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-tachometer-alt"></i>
                    </div>
                    <h3>Realistic Physics</h3>
                    <p>Experience true-to-life car handling with our advanced physics engine that simulates real driving conditions.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-globe-americas"></i>
                    </div>
                    <h3>Multiple Tracks</h3>
                    <p>Race on diverse tracks from city streets to mountain passes, each with unique challenges and scenery.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">
                        <i class="fas fa-trophy"></i>
                    </div>
                    <h3>Competitive Multiplayer</h3>
                    <p>Challenge players from around the world in real-time races and climb the global leaderboards.</p>
                </div>
            </div>
        </div>
    </section>

    <section class="section" id="game">
        <div class="container">
            <div class="section-title">
                <h2>Play Game Demo</h2>
                <p>Try our interactive demo and experience the thrill of CarExGame</p>
            </div>
            <div class="game-demo">
                <div class="score-display">
                    <div class="score">Score: <span id="score">0</span></div>
                    <div class="high-score">High Score: <span id="highScore">0</span></div>
                </div>
                <div class="game-container">
                    <div class="game-road" id="gameRoad">
                        <!-- Road lines will be generated by JavaScript -->
                    </div>
                    <div class="player-car" id="playerCar">
                        <div class="car-window"></div>
                        <div class="car-light left"></div>
                        <div class="car-light right"></div>
                    </div>
                    <div class="game-over" id="gameOver">
                        <h2>Game Over!</h2>
                        <p>Your Score: <span id="finalScore">0</span></p>
                        <button class="btn" id="restartBtn"><i class="fas fa-redo"></i> Play Again</button>
                    </div>
                </div>
                <div class="controls">
                    <button class="control-btn" id="leftBtn"><i class="fas fa-arrow-left"></i> Left</button>
                    <button class="control-btn" id="rightBtn">Right <i class="fas fa-arrow-right"></i></button>
                    <button class="control-btn" id="startBtn"><i class="fas fa-play"></i> Start Game</button>
                </div>
                <div class="game-instructions">
                    <p>Use LEFT and RIGHT arrow keys or buttons to avoid obstacles. The car moves automatically!</p>
                </div>
            </div>
        </div>
    </section>

    <section class="section" id="cars">
        <div class="container">
            <div class="section-title">
                <h2>Available Cars</h2>
                <p>Choose from our collection of high-performance vehicles</p>
            </div>
            <div class="cars">
                <div class="car-card">
                    <div class="car-image">
                        <div class="car-icon"><i class="fas fa-car"></i></div>
                    </div>
                    <h3>Sports Racer</h3>
                    <p>High-speed performance car with excellent handling</p>
                    <div class="car-stats">
                        <div class="stat">
                            <span class="stat-value">9/10</span>
                            <span class="stat-label">Speed</span>
                        </div>
                        <div class="stat">
                            <span class="stat-value">8/10</span>
                            <span class="stat-label">Handling</span>
                        </div>
                        <div class="stat">
                            <span class="stat-value">7/10</span>
                            <span class="stat-label">Acceleration</span>
                        </div>
                    </div>
                </div>
                <div class="car-card">
                    <div class="car-image">
                        <div class="car-icon"><i class="fas fa-car-side"></i></div>
                    </div>
                    <h3>Formula X</h3>
                    <p>Professional racing machine with ultimate speed</p>
                    <div class="car-stats">
                        <div class="stat">
                            <span class="stat-value">10/10</span>
                            <span class="stat-label">Speed</span>
                        </div>
                        <div class="stat">
                            <span class="stat-value">7/10</span>
                            <span class="stat-label">Handling</span>
                        </div>
                        <div class="stat">
                            <span class="stat-value">9/10</span>
                            <span class="stat-label">Acceleration</span>
                        </div>
                    </div>
                </div>
                <div class="car-card">
                    <div class="car-image">
                        <div class="car-icon"><i class="fas fa-truck-monster"></i></div>
                    </div>
                    <h3>Off-Road Beast</h3>
                    <p>Powerful vehicle built for rough terrain</p>
                    <div class="car-stats">
                        <div class="stat">
                            <span class="stat-value">7/10</span>
                            <span class="stat-label">Speed</span>
                        </div>
                        <div class="stat">
                            <span class="stat-value">9/10</span>
                            <span class="stat-label">Handling</span>
                        </div>
                        <div class="stat">
                            <span class="stat-value">8/10</span>
                            <span class="stat-label">Acceleration</span>
                        </div>
                    </div>
                </div>
                <div class="car-card">
                    <div class="car-image">
                        <div class="car-icon"><i class="fas fa-police-car"></i></div>
                    </div>
                    <h3>Police Interceptor</h3>
                    <p>High-performance law enforcement vehicle</p>
                    <div class="car-stats">
                        <div class="stat">
                            <span class="stat-value">8/10</span>
                            <span class="stat-label">Speed</span>
                        </div>
                        <div class="stat">
                            <span class="stat-value">8/10</span>
                            <span class="stat-label">Handling</span>
                        </div>
                        <div class="stat">
                            <span class="stat-value">8/10</span>
                            <span class="stat-label">Acceleration</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <footer id="contact">
        <div class="container">
            <div class="footer-content">
                <div class="footer-column">
                    <h3>CarExGame</h3>
                    <p>The ultimate car racing experience with realistic physics, stunning graphics, and competitive multiplayer.</p>
                    <div class="social-links">
                        <a href="#" class="social-link"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" class="social-link"><i class="fab fa-twitter"></i></a>
                        <a href="#" class="social-link"><i class="fab fa-instagram"></i></a>
                        <a href="#" class="social-link"><i class="fab fa-youtube"></i></a>
                    </div>
                </div>
                <div class="footer-column">
                    <h3>Quick Links</h3>
                    <ul>
                        <li><a href="#home"><i class="fas fa-chevron-right"></i> Home</a></li>
                        <li><a href="#features"><i class="fas fa-chevron-right"></i> Features</a></li>
                        <li><a href="#game"><i class="fas fa-chevron-right"></i> Play Game</a></li>
                        <li><a href="#cars"><i class="fas fa-chevron-right"></i> Cars</a></li>
                    </ul>
                </div>
                <div class="footer-column">
                    <h3>Support</h3>
                    <ul>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Help Center</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Contact Us</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> FAQ</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Community</a></li>
                    </ul>
                </div>
                <div class="footer-column">
                    <h3>Legal</h3>
                    <ul>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Privacy Policy</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Terms of Service</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Cookie Policy</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Copyright</a></li>
                    </ul>
                </div>
            </div>
            <div class="copyright">
                <p>&copy; 2023 CarExGame. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script>
        // Game variables
        let gameActive = false;
        let score = 0;
        let highScore = localStorage.getItem('highScore') || 0;
        let playerPosition = 50; // percentage from left
        let obstacles = [];
        let gameSpeed = 5;
        let gameInterval;
        let roadAnimation;
        
        // DOM elements
        const playerCar = document.getElementById('playerCar');
        const scoreElement = document.getElementById('score');
        const highScoreElement = document.getElementById('highScore');
        const finalScoreElement = document.getElementById('finalScore');
        const leftBtn = document.getElementById('leftBtn');
        const rightBtn = document.getElementById('rightBtn');
        const startBtn = document.getElementById('startBtn');
        const restartBtn = document.getElementById('restartBtn');
        const gameContainer = document.querySelector('.game-container');
        const gameRoad = document.getElementById('gameRoad');
        const gameOverScreen = document.getElementById('gameOver');
        
        // Initialize game
        function initGame() {
            // Set high score
            highScoreElement.textContent = highScore;
            
            // Create road lines
            createRoadLines();
            
            // Set up event listeners
            leftBtn.addEventListener('click', moveLeft);
            rightBtn.addEventListener('click', moveRight);
            startBtn.addEventListener('click', startGame);
            restartBtn.addEventListener('click', startGame);
            
            // Keyboard controls
            document.addEventListener('keydown', (e) => {
                if (e.key === 'ArrowLeft') {
                    moveLeft();
                } else if (e.key === 'ArrowRight') {
                    moveRight();
                } else if (e.key === ' ' && !gameActive) {
                    startGame();
                }
            });
            
            // Initial player position
            updatePlayerPosition();
        }
        
        // Create road lines
        function createRoadLines() {
            for (let i = 0; i < 20; i++) {
                const roadLine = document.createElement('div');
                roadLine.className = 'road-line';
                roadLine.style.top = `${i * 100}px`;
                gameRoad.appendChild(roadLine);
            }
        }
        
        // Move player left
        function moveLeft() {
            if (playerPosition > 15) {
                playerPosition -= 10;
                updatePlayerPosition();
            }
        }
        
        // Move player right
        function moveRight() {
            if (playerPosition < 85) {
                playerPosition += 10;
                updatePlayerPosition();
            }
        }
        
        // Update player car position
        function updatePlayerPosition() {
            playerCar.style.left = `${playerPosition}%`;
        }
        
        // Start the game
        function startGame() {
            if (gameActive) return;
            
            gameActive = true;
            score = 0;
            obstacles = [];
            gameSpeed = 5;
            scoreElement.textContent = score;
            startBtn.innerHTML = '<i class="fas fa-pause"></i> Pause Game';
            gameOverScreen.style.display = 'none';
            
            // Clear any existing obstacles
            document.querySelectorAll('.obstacle').forEach(obs => obs.remove());
            
            // Start road animation
            gameRoad.style.animationDuration = `${3 - gameSpeed/10}s`;
            
            // Start game loop
            clearInterval(gameInterval);
            gameInterval = setInterval(gameLoop, 50);
        }
        
        // Main game loop
        function gameLoop() {
            if (!gameActive) return;
            
            // Move obstacles
            moveObstacles();
            
            // Create new obstacles randomly
            if (Math.random() < 0.03) {
                createObstacle();
            }
            
            // Check collisions
            checkCollisions();
            
            // Increase score
            score++;
            scoreElement.textContent = score;
            
            // Increase difficulty
            if (score % 100 === 0) {
                gameSpeed += 0.5;
                gameRoad.style.animationDuration = `${3 - gameSpeed/10}s`;
            }
        }
        
        // Create a new obstacle
        function createObstacle() {
            const obstacle = document.createElement('div');
            obstacle.className = 'obstacle';
            
            // Random position (20% to 80%)
            const position = Math.floor(Math.random() * 60) + 20;
            obstacle.style.left = `${position}%`;
            obstacle.style.top = `-150px`;
            
            gameContainer.appendChild(obstacle);
            obstacles.push({
                element: obstacle,
                position: position,
                top: -150
            });
        }
        
        // Move all obstacles
        function moveObstacles() {
            for (let i = obstacles.length - 1; i >= 0; i--) {
                const obstacle = obstacles[i];
                obstacle.top += gameSpeed;
                obstacle.element.style.top = `${obstacle.top}px`;
                
                // Remove obstacles that are off screen
                if (obstacle.top > gameContainer.offsetHeight) {
                    gameContainer.removeChild(obstacle.element);
                    obstacles.splice(i, 1);
                }
            }
        }
        
        // Check for collisions
        function checkCollisions() {
            const playerRect = playerCar.getBoundingClientRect();
            
            for (const obstacle of obstacles) {
                const obstacleRect = obstacle.element.getBoundingClientRect();
                
                // Simple collision detection
                if (playerRect.left < obstacleRect.right &&
                    playerRect.right > obstacleRect.left &&
                    playerRect.top < obstacleRect.bottom &&
                    playerRect.bottom > obstacleRect.top) {
                    
                    // Collision detected
                    gameOver();
                    return;
                }
            }
        }
        
        // Game over
        function gameOver() {
            gameActive = false;
            clearInterval(gameInterval);
            
            // Update high score
            if (score > highScore) {
                highScore = score;
                localStorage.setItem('highScore', highScore);
                highScoreElement.textContent = highScore;
            }
            
            // Show game over screen
            finalScoreElement.textContent = score;
            gameOverScreen.style.display = 'flex';
        }
        
        // Initialize the game when page loads
        window.addEventListener('load', initGame);
    </script>
</body>
</html>
